# RESULTS (learned) — Stage 3 model vs heuristic (Section 21)

> Generated by `scripts/fit_learned_statsbomb.py` on competition 43, season 3 (64 matches, 4 folds). Held-out (future-block) metrics, walk-forward.

| Model | Held-out log loss | Brier |
|---|---|---|
| Untuned heuristic | 0.5452 | 0.1797 |
| Fitted heuristic (base_rate/tau) | 0.5449 | 0.1796 |
| Learned logistic model | 0.5550 | 0.1818 |

## Verdict

- Learned model beats the fitted heuristic out of sample: **False** (0.5449 vs 0.5550).

> The learned model does not beat the fitted heuristic here. With this feature set and sample size the hand-built intensity is already near the achievable signal; a learned model only earns its place when it beats the heuristic out of sample (Section 21) — and it must be re-checked as more data and richer features (e.g. player avatars) are added.
