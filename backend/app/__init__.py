"""Football Intelligence Engine — API service.

Depends on the ``fie`` engine library (src/fie) for all football logic; this
package owns only persistence (Section 6, production schema) and the HTTP layer.
"""
